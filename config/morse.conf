# /etc/modprobe.d/morse.conf
# Heltec HT-HC01P (Morse Micro MM6108): board calibration file + regulatory domain.
# The driver's build default is country=AU — this must say US or TX is illegal here.
options morse bcf=bcf_mf08551.bin country=US
